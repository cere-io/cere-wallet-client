import { observer } from 'mobx-react-lite';
import { Stack } from '@cere-wallet/ui';

const TransferCollectibles = () => {
  return (
    <Stack alignItems="stretch" spacing={2}>
      TransferCollectibles
    </Stack>
  );
};

export default observer(TransferCollectibles);
